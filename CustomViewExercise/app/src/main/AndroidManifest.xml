<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.canlioya.customviewexercise">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".carousellayoutmanager.CustomLayoutManagerActivity"/>
        <activity android:name=".donut.FancyViewsActivity" />
        <activity android:name=".linededittext.NotePadActivity" />
        <activity android:name=".bookshelflayout.BookShelfActivity" />
        <activity android:name=".dynamicpropertychange.DynamicPropertyChangeActivity" />
        <activity android:name=".diagonallayout.DiagonalLayoutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>